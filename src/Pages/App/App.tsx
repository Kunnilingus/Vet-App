import { FC } from "react";
import styles from "./app.module.scss";
import Header from "@/components/Header/Header";

export const App: FC = () => {
  return (
    <div>
      <Header />
    </div>
  );
};
